import React, { FC } from "react";

export type AlignmentCellProps = {};

export const AlignmentCell: FC<AlignmentCellProps> = ({}) => {
  return (
    <section>
      <div></div>
    </section>
  );
};
